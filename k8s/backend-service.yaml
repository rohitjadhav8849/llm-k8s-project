apiVersion: v1
kind: Service
metadata:
  name: llm-backend-service
spec:
  type: NodePort
  selector:
    app: llm-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30007